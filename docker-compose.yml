services:
  kinoko_api:
    # image: kinoko_api
    build: 
      dockerfile: ./api.Dockerfile
    ports:
    - 8000:8080
    restart: unless-stopped
    environment:
    - PORT=8080

  kinoko_streamlit:
    # image: kinoko_streamlit
    build: 
      dockerfile: ./streamlit.Dockerfile
    ports: 
    - 8015:8501
    restart: unless-stopped
